language: node_js

node_js: stable

before_install:
    - export TZ='Asia/Shanghai'
    - npm install hexo-cli -g
    - npm install hexo -g

install:
    - npm install

script:
    - hexo clean

after_script:
    - cd tzzs/tzzs.githu.io
    - git checkout hexo
    - hexo g
    - ls

    - git config user.name "tzzs"
    - git config user.email "1789803837@qq.com"

    - git checkout master
    - ls
    - git add .
    - git commit -m "Travis CI Auto Builder at `date +"%Y-%m-%d %H:%M"`"
    - git push --force "https://${blog}@${GH_REF}"
    # - git push --force "https://tzzchn@outlook.com:${CodingToken}@${Coding}" master:master

branches:
    only:
        - hexo

env:
    global:
        - GH_REF: github.com/tzzs/tzzs.github.io.git
        - Coding: e.coding.net/imtzz/Pages.git
